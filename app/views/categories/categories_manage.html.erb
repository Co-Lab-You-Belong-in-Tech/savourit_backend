<h1> Merge categories </h1>
<p>All meal in the first category will be moved to the second category and the first category will be destroyed</p>
<%= form_tag merge_categories_path do %>
  <%= select_tag "category1", options_from_collection_for_select(@categories1, "id", "name")%> ==> 
  <%= select_tag "category2", options_from_collection_for_select(@categories2, "id", "name")%>
  <%= submit_tag %>
<% end %>
