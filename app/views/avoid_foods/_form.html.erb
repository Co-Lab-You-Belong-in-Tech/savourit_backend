<%= form_with(model: avoid_food) do |form| %>
  <% if avoid_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(avoid_food.errors.count, "error") %> prohibited this avoid_food from being saved:</h2>

      <ul>
        <% avoid_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :avoid_food_category_id, style: "display: block" %>
    <%= form.text_field :avoid_food_category_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
